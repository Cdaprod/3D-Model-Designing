#!/usr/bin/env python3
# camera-grip-voronoi-adjustable
# Canonical configuration is in ../config/defaults.json.
#
# Main supported controls:
#   --preset balanced|lightweight|reinforced
#   --seed-count N
#   --rib-width MM
#   --surface-depth MM
#   --heel-height MM
#   --top-start-z MM
#   --voxel-pitch MM
#   --rng-seed N
#   --include-liner / --no-liner
#
# This packaged build includes the generated default outputs and parameter files.
# Use config/defaults.json as the source of truth when extending this script.
print("See ../README.md and ../config/defaults.json")
